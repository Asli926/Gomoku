#include <cstdio>

#include "../include/HumanStrategy.h"

bool HumanStrategy::GetStrategy(Board *board, int player_num, int *px, int *py) {
    int state;

    printf("Please input the valid coordinates of next chess. (row col)\n");
    scanf("%d %d", px, py);
}